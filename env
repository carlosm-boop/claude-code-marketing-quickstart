# Marketing OS — environment variables
#
# Copy this file to .env and fill in real values. .env is gitignored so secrets stay local.
# This file (`env`, no extension, no leading dot) is committed as a template so anyone cloning
# the repo can see what they need to set without it being hidden as a dotfile.
#
# NOTHING IN THIS FILE IS REQUIRED. Claude Code's built-in WebSearch/WebFetch cover every
# research skill, and Gmail / Calendar / Drive / Slack authenticate through claude.ai, not here.
# Full picture: .claude/connections.md

# === OPTIONAL — Exa ===
# Overlaps origami.chat, which you already use. Built-in WebSearch/WebFetch cover this repo's
# research skills. Only set this if a free-tier trial beats origami on something specific.
# EXA_API_KEY=

# === OPTIONAL — uncomment as you wire each MCP ===

# Google Analytics 4
# Property ID is in your GA4 admin panel (looks like "properties/123456789")
# Credentials JSON is from a Google Cloud service account with GA4 access
# GA4_PROPERTY_ID=
# GA4_CREDENTIALS_JSON=

# Google Search Console
# Site URL exactly matches the verified property in GSC (e.g., "sc-domain:yourdomain.com")
# GSC_SITE_URL=
# GSC_CREDENTIALS_JSON=

# Granola — meeting transcripts
# Get API key from your Granola account settings
# GRANOLA_API_KEY=

# Slack — team notifications + channel reads
# Bot token starts with "xoxb-"; install Slack app via api.slack.com first
# SLACK_BOT_TOKEN=

# Firecrawl — heavy web crawl
# Get API key from firecrawl.dev
# FIRECRAWL_API_KEY

# ==========================================================================
# WeKan.AI stack — rev. 2026-08-31
# ==========================================================================
# Checked against the connector registry: origami.chat, TheirStack, HubSpot,
# SmartLead, HeyReach and Customer.io have NO MCP connector. They are reached
# by manual export/paste or an n8n / Make webhook bridge, so their API keys
# live in n8n/Make — NOT here.
#
# Gmail, Google Calendar, Google Drive and Slack authenticate through
# claude.ai. Google Meet transcripts arrive via the Drive connector.
#
# Retired from this repo: Granola, Gong, Notion, Linear, Ahrefs, Firecrawl, GA4.

# --- Only when SEO work starts ---
# Until this is set, /seo-strategy returns [UNAVAILABLE] for every search volume.
# Site URL must exactly match the verified property, e.g. "sc-domain:wekancode.com"
# GSC_SITE_URL=
# GSC_CREDENTIALS_JSON=

# --- Only when SmartLead / HeyReach go live ---
# Webhook targets for reviewed artifacts leaving the repo. Manual trigger only —
# the review gate in marketing/rules/escalation.md stays in front of every send.
# N8N_WEBHOOK_SMARTLEAD=
# N8N_WEBHOOK_HEYREACH=
# N8N_WEBHOOK_HUBSPOT=
# N8N_WEBHOOK_CUSTOMERIO=
